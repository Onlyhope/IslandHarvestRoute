<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.aaron.islandharvestroute.RouteMainActivity"
    tools:showIn="@layout/activity_route_main">


    <TextView
        android:id="@+id/endAddressLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/endAddressLabel"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/endAddressTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/endAddressLabel"
        android:text="@string/endAddressText"
        android:textSize="18sp" />

    <ImageView
        android:id="@+id/mapImageView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@id/endAddressTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:src="@drawable/placeholder" />

    <Button

        android:id="@+id/routeDetailButton"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/mapImageView"
        android:layout_marginTop="20dp"
        android:text="Route" />

    <Button

        android:id="@+id/dataEntryButton"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:layout_alignTop="@+id/routeDetailButton"
        android:layout_toEndOf="@+id/routeDetailButton"
        android:layout_toRightOf="@+id/routeDetailButton"
        android:text="Submit Info" />

    <Button

        android:id="@+id/completeButton"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:layout_alignBottom="@+id/dataEntryButton"
        android:layout_toEndOf="@+id/dataEntryButton"
        android:layout_toRightOf="@+id/dataEntryButton"
        android:text="Complete" />


</RelativeLayout>
